// Deprecated: this repo doesn't ship with a test runner.
//
// Keeping a `*.test.ts` file around can confuse tooling into expecting jest/vitest
// types to exist. If you need a quick manual check, use `lib/blogExport.smoke.ts`.
